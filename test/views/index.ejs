<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Repositories</title>
    <link rel="stylesheet" type="text/css" href="test\public">

</head>
<body>
    <h1>Repositories for <%= GITHUB_USERNAME %></h1>
    <ul>
        <% repos.forEach(repo => { %>
            <li>
                <a href="/repos/<%= repo.name %>"><%= repo.name %></a>
            </li>
        <% }) %>
    </ul>
</body>
</html>
